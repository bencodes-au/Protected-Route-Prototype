name: 'Parse Test Logs'
description: 'Parse and process test logs file'
inputs:
  log-path:
    description: 'Path to the test log file'
    required: true
runs:
  using: 'node20'
  main: 'parse-tests.js'