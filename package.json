{
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "mongodb-memory-server": "^10.1.4",
    "supertest": "^7.1.1"
  },
  "scripts": {
  "test": "cross-env NODE_ENV=test jest --coverage --outputFile=logs/test-report.txt --json",
  "start": "node server.js",
  "dev": "nodemon server.js"
}
}
